<resources>

    <style name="AppTheme" parent="HomeDashboardTheme.Base"></style>

    <style name="HomeDashboardTheme.Base" parent="Theme.AppCompat.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="android:background">@color/black</item>

        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/ambient_white</item>
        <item name="colorPrimaryDark">@color/navigation_bar</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

</resources>